import React from 'react';
import Typography from '../../../common/components/Typography';

const Logo = () => (
  <Typography fontSize={24} fontWeight={700} color="accent" as="h1">
    Slothy.io
  </Typography>
);

export default Logo;
