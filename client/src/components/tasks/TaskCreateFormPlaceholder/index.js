import React from "react";
import styles from "./TaskCreateFormPlaceholder.scss";

const TaskCreateFormPlaceholder = () => (
    <span className={styles.TaskCreateFormPlaceholder}>

    </span>
);

export default TaskCreateFormPlaceholder;