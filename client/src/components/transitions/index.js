import React from "react";
import { CSSTransition } from "react-transition-group";
import styles from "./Transitions.scss";

export const FadeInScale = ({ children, timeout, shouldStart }) => {
    return 
}
