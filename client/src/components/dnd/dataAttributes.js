export const prefix = 'data-react';
export const dragHandle = `${prefix}-drag-handle`;
export const draggable = `${prefix}-draggable`;
export const droppable = `${prefix}-droppable`;
export const placeholder = `${prefix}-placeholder`;
